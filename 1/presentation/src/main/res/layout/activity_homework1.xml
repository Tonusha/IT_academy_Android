<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
<data>
    <import type="android.view.View" />
    <variable
        name="viewModel"
        type="by.nca.it_academy.work_mvvm.hw1.UserViewModel"></variable>
</data>
<LinearLayout
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#77eddaf4"
    android:orientation="vertical">
    <TextView
        android:layout_width="123dp"
        android:layout_height="32dp"
        android:clickable="true"
        android:background="#77c962ef"
        android:text="@{viewModel.text1}"
        android:textAppearance="@style/TextAppearance.AppCompat" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="32dp"
        android:background="#77bf40ed"
        android:clickable="true"
        android:text="@{viewModel.text2}"
        android:textAppearance="@style/TextAppearance.AppCompat" />

    <!--<Button-->
        <!--android:id="@+id/button1"-->
        <!--style="@android:style/Widget.DeviceDefault.Light.Button.Borderless.Small"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:background="#4503"-->
        <!--android:text="@{viewModel::buttonClick}" />-->

</LinearLayout>
</layout>